numbers  from fizzbuzz as numbers # a sequence of numbers
even     from fizzbuzz as even # a filter for even numbers
fizzbuzz from fizzbuzz as fizzbuzz
printer  from fizzbuzz as printer

numbers
/> even
|> fizzbuzz
|> printer
